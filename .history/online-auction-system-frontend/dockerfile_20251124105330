# ---- BUILD STAGE ----
FROM node:22 AS builder

WORKDIR /app
COPY . .
RUN npm install
RUN npm run build

# ---- RUN STAGE ----
FROM nginx:latest

# Copy build output
COPY --from=builder /app/dist /usr/share/nginx/html

# Expose port
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
